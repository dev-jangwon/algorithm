# include <cstdio>
# include <vector>

using namespace std;

int main(void) {
  int N;
  pair<int, int> seven25;
  vector<pair<int, int> > cs;

  scanf("%d %d", &seven25.first, %seven25.second);
  scanf("%d", &N);
  cs.resize(N);
  for (int i = 0; i < N; i++) {
    scanf("%d %d", &cs[i].first, &cs[i].second);
  }
  return 0;
}
